# Dependencies
node_modules/

# Build output
dist/
dist-ssr/
*.local

# Rust / Tauri
src-tauri/target/
src-tauri/gen/schemas/

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Editors
.vscode/*
!.vscode/extensions.json
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini

# Local working files (kept out of the published repo on purpose)
.claude/
CLAUDE.md
mockup.html

# Note: package-lock.json and src-tauri/Cargo.lock are intentionally NOT ignored.
# Both are lockfiles for an application (not a library) and belong in version control
# so builds are reproducible.
